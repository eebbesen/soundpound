{
  "name": "soundpound",
  "engines": {
    "node": ">=12.13.1"
  },
  "version": "0.1.1",
  "description": "keyboard-driven sound and light show",
  "scripts": {
    "start": "harp server --port $PORT"
  },
  "dependencies": {
    "bl": ">=2.2.1",
    "clean-css": ">=4.1.11",
    "cli": ">=1.0.0",
    "harp": "*",
    "yargs-parser": ">=13.1.2"
  }
}
